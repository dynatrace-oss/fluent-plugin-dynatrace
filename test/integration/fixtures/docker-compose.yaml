services:
  fluent:
    build:
      context: ../../../
      dockerfile: test/integration/fixtures/fluent/Dockerfile
    ports:
      - 8080:8080
    links:
      - logsink
  logsink:
    build: logsink
    expose:
      - 8080
